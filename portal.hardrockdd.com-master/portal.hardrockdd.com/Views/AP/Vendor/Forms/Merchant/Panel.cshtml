@model portal.Models.Views.AP.Vendor.Forms.VendorMerchantViewModel

@{
    var id = "a" + Guid.NewGuid().ToString().Replace("-", "");
}
<div id="accordion" class="accordion">
    <!-- begin card -->
    <div class="card" style="overflow:visible;">
        <div class="card-header bg-dark-darker pointer-cursor d-flex align-items-center" data-toggle="collapse" data-target="#@id">
            <i class="fa fa-circle fa-fw text-blue mr-2 f-s-8"></i> Merchant Info
        </div>
        <div id="@id" class="collapse">
            <div class="card-body">
                @Html.Partial("../AP/Vendor/Forms/Merchant/Form", Model)
            </div>
        </div>
    </div>
    <!-- end card -->
</div>