@model portal.Models.Views.HQ.Batch.AP.APFormViewModel
@{
    var panelId = string.Format("panel_{0}", Guid.NewGuid());
}

@Html.Partial("../HQ/Batch/Buttons/Form", Model.Buttons)
@if (Model.BatchTransactions != null)
{
    @Html.Partial("../HQ/Batch/Detail/AP/Batch/Panel", Model.BatchTransactions)
}
@if (Model.PostedTransactions != null)
{
    @Html.Partial("../HQ/Batch/Detail/AP/Posted/Panel", Model.PostedTransactions)
}
