@model portal.Models.Views.HQ.Batch.AP.APBatchLineListViewModel
@{
    ViewBag.TableRow = "True";
    var tableId = string.Format("table_{0}", Guid.NewGuid());
    var tableLayout = new TableLayoutModel(this);
    var viewOnly = bool.TryParse(ViewBag.ViewOnly?.ToString(), out bool viewOnlyOut) ? viewOnlyOut : false;
    string tableKey = tableLayout.TableKey;
}

<table class="table"
       style="display:none"
       id="@tableId"
       data-urlselect="@Url.Action("APBatchLinePanel", "Batch")"
       data-dstselect="lineform"
       data-tablekey="@tableKey">
    <thead>
        <tr class="">
            <th width="5%" data-datatable-filter-type="input">@Html.LabelFor(model => model.List.FirstOrDefault().APLineId)</th>
            <th width="10%" data-datatable-filter-type="multiselect">@Html.LabelFor(model => model.List.FirstOrDefault().LineTypeId)</th>
            <th width="20%" data-datatable-filter-type="input">@Html.LabelFor(model => model.List.FirstOrDefault().Description)</th>
            <th width="20%" data-datatable-filter-type="input">@Html.LabelFor(model => model.List.FirstOrDefault().JobId)</th>
            <th width="20%" data-datatable-filter-type="input">@Html.LabelFor(model => model.List.FirstOrDefault().EquipmentId)</th>
            <th width="10%" data-datatable-filter-type="none">@Html.LabelFor(model => model.List.FirstOrDefault().GrossAmt)</th>
            
        </tr>
    </thead>
    <tbody>
        @foreach (var obj in Model.List)
        {
            @Html.Partial("../HQ/Batch/Detail/AP/Batch/Line/List/TableRow", obj)
        }
    </tbody>
</table>
<script>
$('#@tableId').ready(function () {
    var table = $('#@tableId');
    var h = 42 * @(Model.List.Count);
    if (h > 500) {
        h = 500;
        var parent = $(table).parent();
        var contaner = $('<div></div>');
        $(contaner).css("height", h + "px");
        $(contaner).css("overflow-y", "scroll");
        $(contaner).css("display", "block");

        $(contaner).appendTo(parent);
        $(table).appendTo(contaner);
    }
    $(table).show();
    Table().init($('#@tableId'));
    Table().selectFirstRow($('#@tableId'));
});
</script>