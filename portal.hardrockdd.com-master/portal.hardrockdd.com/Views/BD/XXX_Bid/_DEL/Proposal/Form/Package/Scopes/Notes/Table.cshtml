<!--@model  portal.Models.Views.Bid.BidPackageProposalScopeListViewModel
@{
    ViewBag.TableRow = "True";
    var tableId = string.Format("table_{0}", Guid.NewGuid());
    var tableLayout = new TableLayoutModel(this);
    var viewOnly = bool.TryParse(ViewBag.ViewOnly?.ToString(), out bool viewOnlyOut) ? viewOnlyOut : false;
    string tableKey = "";
    foreach (var prp in tableLayout.Properties)
    {
        if (prp.IsKey)
        {
            var myData = Model.GetType().GetProperty(prp.Property);
            var myVal = myData.GetValue(Model, null);
            if (tableKey != string.Empty)
            {
                tableKey += "&";
            }
            tableKey += prp.Property + "=" + myVal.ToString();
        }
    }
}
<h3>Package Clarifications</h3>
<table class="table"
       id="@tableId"
       data-urlcreate="@Url.Action("AddNote", "BidPackageProposalScope")"
       data-urldelete="@Url.Action("Delete", "BidPackageProposalScope")"
       data-urlupdate="@Url.Action("Update", "BidPackageProposalScope")"
       data-urlvalidate="@Url.Action("Validate", "BidPackageProposalScope")"-->
       @*data-urlselect="@Url.Action("Panel", "BidBoreLinePass"), @Url.Action("Panel", "BidBoreLineCostItem")"
       data-dstselect="BidBoreLineInfo"
       data-dstselectvalidate="false"*@
       <!--data-tablekey="@tableKey">
    <thead>
        <tr class="">
            <th width="25%">@Html.LabelForRequired(model => model.List.FirstOrDefault().Title)</th>
            <th>@Html.LabelForRequired(model => model.List.FirstOrDefault().Notes)</th>
            @if (!viewOnly)
            {
                <th>
                    <button class="btn btn-primary addRow pull-right">
                        <i class="fa fa-plus"></i>
                    </button>
                </th>
            }
        </tr>
    </thead>
    <tbody>
        @foreach (var obj in Model.List.Where(f => f.ScopeTypeId == (byte)DB.ScopeTypeEnum.Note).ToList())
        {
            @Html.Partial("../BD/Bid/Proposal/Form/Package/Scopes/Notes/TableRow", obj)
        }
    </tbody>
</table>
<script>

</script>-->