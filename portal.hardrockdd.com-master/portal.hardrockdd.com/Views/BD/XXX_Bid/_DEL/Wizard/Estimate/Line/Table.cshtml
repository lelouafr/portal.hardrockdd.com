@model  portal.Models.Views.Bid.Wizard.Estimate.BidBoreLineListViewModel
@{
    ViewBag.TableRow = "True";
    var tableId = string.Format("table_{0}", Guid.NewGuid());
    var tableLayout = new TableLayoutModel(this);
    var viewOnly = bool.TryParse(ViewBag.ViewOnly?.ToString(), out bool viewOnlyOut) ? viewOnlyOut : false;
    string tableKey = "";
    foreach (var prp in tableLayout.Properties)
    {
        if (prp.IsKey)
        {
            var myData = Model.GetType().GetProperty(prp.Property);
            var myVal = myData.GetValue(Model, null);
            if (tableKey != string.Empty)
            {
                tableKey += "&";
            }
            tableKey += prp.Property + "=" + myVal.ToString();
        }
    }
}

<table class="table dataTable"
       id="@tableId"
       data-urlcreate="@Url.Action("Add", "BidBoreLineEstimateWizard")"
       data-urldelete="@Url.Action("Delete", "BidBoreLineEstimateWizard")"
       data-urlupdate="@Url.Action("Update", "BidBoreLineEstimateWizard")"
       data-urlvalidate="@Url.Action("Validate", "BidBoreLineEstimateWizard")"
       data-tablekey="@tableKey">
    <thead>
        <tr class="">
            <th width="15%">@Html.LabelForRequired(model => model.List.FirstOrDefault().Description)</th>
            <th width="15%">@Html.LabelForRequired(model => model.List.FirstOrDefault().BoreTypeId)</th>
            <th width="8%">@Html.LabelForRequired(model => model.List.FirstOrDefault().Footage)</th>
            <th width="9%">@Html.LabelForRequired(model => model.List.FirstOrDefault().PipeSize)</th>
            <th width="10%">@Html.LabelForRequired(model => model.List.FirstOrDefault().GroundDensityId)</th>
            <th width="20%">@Html.LabelForRequired(model => model.List.FirstOrDefault().RigCategoryId)</th>
            <th width="8%">@Html.LabelForRequired(model => model.List.FirstOrDefault().CrewCount)</th>
            @if (!viewOnly)
            {
                <th width="15%">
                    <button class="btn btn-primary addRow pull-right">
                        <i class="fa fa-plus"></i>
                    </button>
                </th>
            }
        </tr>
    </thead>
    <tbody>
        @foreach (var obj in Model.List)
        {
            @Html.Partial("../BidWizard/Estimate/BidBoreLine/TableRow", obj)
        }
    </tbody>
</table>
<script>
    //$('#example').arrowTable();

</script>