@model  portal.Models.Views.Bid.Wizard.Award.PackageListViewModel
@{
    ViewBag.TableRow = "True";
    var tableId = string.Format("table_{0}", Guid.NewGuid());
    var tableLayout = new TableLayoutModel(this);
    var viewOnly = bool.TryParse(ViewBag.ViewOnly?.ToString(), out bool viewOnlyOut) ? viewOnlyOut : false;
    string tableKey = "";
    foreach (var prp in tableLayout.Properties)
    {
        if (prp.IsKey)
        {
            var myData = Model.GetType().GetProperty(prp.Property);
            var myVal = myData.GetValue(Model, null);
            if (tableKey != string.Empty)
            {
                tableKey += "&";
            }
            tableKey += prp.Property + "=" + myVal.ToString();
        }
    }
}

<table class="table"
       id="@tableId"
       data-urlcreate="@Url.Action("Add", "BidPackageAwardWizard")"
       data-urldelete="@Url.Action("Delete", "BidPackageAwardWizard")"
       data-urlupdate="@Url.Action("Update", "BidPackageAwardWizard")"
       data-urlvalidate="@Url.Action("Validate", "BidPackageAwardWizard")"
       data-urlselect="@Url.Action("Panel", "BidBoreLineAwardWizard")"
       data-dstselect="BidBoreLineSelect"
       data-dstselectvalidate="false"
       data-tablekey="@tableKey">
    <thead>
        <tr class="">
            <th width="40%">@Html.LabelForRequired(model => model.List.FirstOrDefault().Description)</th>
            <th width="15%">@Html.LabelForRequired(model => model.List.FirstOrDefault().DivisionId)</th>
            <th width="20%">@Html.LabelForRequired(model => model.List.FirstOrDefault().CustomerId)</th>
            <th width="20%">@Html.LabelForRequired(model => model.List.FirstOrDefault().Status)</th>

        </tr>
    </thead>
    <tbody>
        @foreach (var obj in Model.List)
        {
            @Html.Partial("../BD/Bid/Wizard/Award/Package/List/TableRow", obj)
        }
    </tbody>
</table>

<script>
    $('#@tableId').ready(function () {
        $('#@tableId').arrowTable();
    })
</script>