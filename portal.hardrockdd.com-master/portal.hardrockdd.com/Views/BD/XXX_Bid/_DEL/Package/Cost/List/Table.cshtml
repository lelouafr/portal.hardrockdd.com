@model  portal.Models.Views.Bid.BidPackageCostItemListViewModel
@{
    ViewBag.TableRow = "True";
    var tableId = string.Format("table_{0}", Guid.NewGuid());
    var tableLayout = new TableLayoutModel(this);
    var viewOnly = bool.TryParse(ViewBag.ViewOnly?.ToString(), out bool viewOnlyOut) ? viewOnlyOut : false;
    string tableKey = "";
    foreach (var prp in tableLayout.Properties)
    {
        if (prp.IsKey)
        {
            var myData = Model.GetType().GetProperty(prp.Property);
            var myVal = myData.GetValue(Model, null);
            if (tableKey != string.Empty)
            {
                tableKey += "&";
            }
            tableKey += prp.Property + "=" + myVal.ToString();
        }
    }
}

<table class="table"
       id="@tableId"
       data-urlcreate="@Url.Action("Add", "BidPackageCostItem")"
       data-urldelete="@Url.Action("Delete", "BidPackageCostItem")"
       data-urlupdate="@Url.Action("Update", "BidPackageCostItem")"
       @*data-urlvalidate="@Url.Action("Validate", "BidPackageCostItem")"*@
       data-urlselect=""
       data-dstselect=""
       data-dstselectvalidate=""
       data-tablekey="@tableKey">
    <thead>
        <tr class="">
            <th width="50%">@Html.LabelForRequired(model => model.List.FirstOrDefault().BudgetCodeId)</th>
            <th width="20%">@Html.LabelForRequired(model => model.List.FirstOrDefault().Multiplier)</th>
            <th width="15%">@Html.LabelForRequired(model => model.List.FirstOrDefault().Applied)</th>
            @if (!viewOnly)
            {
                <th width="15%">
                    <button class="btn btn-primary addRow pull-right">
                        <i class="fa fa-plus"></i>
                    </button>
                </th>
            }
        </tr>
    </thead>
    <tbody>
        @foreach (var obj in Model.List)
        {
            @Html.Partial("../BD/Bid/Package/Cost/List/TableRow", obj)
        }
    </tbody>
</table>
<script>

</script>