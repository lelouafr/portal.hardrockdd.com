@model portal.Models.Views.Dashboard.HomeViewModel
@if (User.HasAccess("PORequest"))
{
    <!-- end col-3 -->
    <div class="col-xl-2 col-lg-3 col-md-4">
        <!-- begin card -->
        <div class="card card-inverse bg-gradient-black">
            <div class="card-block">
                <h4 class="card-title m-t-5 m-b-10">Request a PO</h4>
                <p class="card-text m-b-15">Request a PO through the portal.</p>
                <a href="@Url.Action("Create","PORequest")" class="btn btn-sm btn-warning">Create Request</a>
            </div>
        </div>
    </div>
}