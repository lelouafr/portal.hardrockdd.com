@model portal.Models.Views.Dashboard.HomeViewModel
@if (Model.POApprovalCount > 0)
{
    <div class="col-xl-2 col-lg-3 col-md-4">
        <div class="widget widget-stats bg-aqua">
            <div class="stats-icon"><i class="fas fa-clipboard-list"></i></div>
            <div class="stats-info">
                <h4>PO Request Pending Approvals</h4>
                <p>@Model.POApprovalCount</p>
            </div>
            <div class="stats-link">
                <a href="@Url.Action("Index", "PORequestApproval")">Goto Review <i class="fa fa-arrow-alt-circle-right"></i></a>
            </div>
        </div>
    </div>
}