@model portal.Models.Views.HR.Request.Position.CreateRequestViewModel

@{
    var formId = string.Format("form_{0}", Guid.NewGuid());
    var id = formId.ToString().Replace("-", "");
    var viewOnly = bool.TryParse(ViewBag.ViewOnly?.ToString(), out bool viewOnlyOut) ? viewOnlyOut : false;
}

<form class="form-horizontal"
      id="@formId"
      data-url="@Url.Action("Create","HRPositionRequestForm")">
    @Html.AntiForgeryToken()
    @Html.HiddenFor(m => m.HRCo, new { @id = Guid.NewGuid() })
    @Html.HiddenFor(m => m.PRCo, new { @id = Guid.NewGuid() })
    @Html.HiddenFor(m => m.RequestedDate, new { @id = Guid.NewGuid() })
    @Html.HiddenFor(m => m.RequestedBy, new { @id = Guid.NewGuid() })

    <div class="form-group has-feedback row">
        @Html.EditorFor(m => m.PositionCodeId)
    </div>
    <div class="form-group has-feedback row">
        @Html.EditorFor(m => m.ForCrewId)
    </div>
    <div class="form-group has-feedback row">
        @Html.EditorFor(m => m.PriorEmployeeId)
    </div>
    <div class="form-group has-feedback row">
        @Html.EditorFor(m => m.Comments)
    </div>
</form>
