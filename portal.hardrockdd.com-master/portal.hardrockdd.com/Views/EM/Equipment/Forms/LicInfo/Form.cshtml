@model portal.Models.Views.Equipment.Forms.EquipmentLicInfoViewModel

@{
    var viewOnly = bool.TryParse(ViewBag.ViewOnly?.ToString(), out bool viewOnlyOut) ? viewOnlyOut : false;
    var formLayout = new TableLayoutModel(this);
    var entityKey = formLayout.TableKey;
    var formId = string.Format("form_{0}", Guid.NewGuid());
}

<form class="form-horizontal"
      id="@formId"
      data-entitykey="@entityKey"
      data-urlupdate="@Url.Action("UpdateLicInfo", "EquipmentForm")"
      data-urlvalidate="@Url.Action("ValidateLicInfo", "EquipmentForm")">
    @Html.AntiForgeryToken()
    <div class="form-group has-feedback">
        <div class="validation-summary-valid text-danger" data-valmsg-summary="true">
        </div>
    </div>
    @Html.HiddenFor(m => m.EMCo, new { @id = Guid.NewGuid() })
    @Html.HiddenFor(m => m.EquipmentId, new { @id = Guid.NewGuid() })
    <div class="form-group has-feedback row">
        @Html.EditorFor(m => m.LicensePlateNo)
    </div>
    <div class="form-group has-feedback row">
        @Html.EditorFor(m => m.LicensePlateState)
    </div>
    <div class="form-group has-feedback row">
        @Html.EditorFor(m => m.LicensePlateExpDate)
    </div>
    <div class="form-group has-feedback row">
        @Html.EditorFor(m => m.InspectionExpDate)
    </div>
    <div class="form-group has-feedback row">
        @Html.EditorFor(m => m.RegGVWR)
    </div>
</form>
<script>
    $('#@formId').ready(function () { Form().init($('#@formId')); });
</script>