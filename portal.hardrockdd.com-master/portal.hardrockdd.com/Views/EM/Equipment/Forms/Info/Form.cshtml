@model portal.Models.Views.Equipment.Forms.EquipmentInfoViewModel

@{
    var viewOnly = bool.TryParse(ViewBag.ViewOnly?.ToString(), out bool viewOnlyOut) ? viewOnlyOut : false;
    var formLayout = new TableLayoutModel(this);
    var entityKey = formLayout.TableKey;
    var formId = string.Format("form_{0}", Guid.NewGuid());
}

<form class="form-horizontal"
      id="@formId"
      data-entitykey="@entityKey"
      data-urlupdate="@Url.Action("UpdateInfo", "EquipmentForm")"
      data-urlvalidate="@Url.Action("ValidateInfo", "EquipmentForm")">
    @Html.AntiForgeryToken()
    <div class="form-group has-feedback">
        <div class="validation-summary-valid text-danger" data-valmsg-summary="true">
        </div>
    </div>
    @Html.HiddenFor(m => m.EMCo, new { @id = Guid.NewGuid() })

    @foreach (var prp in this.ViewData.ModelMetadata.Properties.Where(p => p.TemplateHint == "HiddenInput").Select(s => s.PropertyName))
    {
        @Html.Hidden(prp, Model.GetType().GetProperty(prp).GetValue(Model, null), new { @id = Guid.NewGuid() })
    }

    <div class="form-group has-feedback row">
        @Html.EditorFor(m => m.EquipmentId, new { ViewOnly = true })
        @Html.EditorFor(m => m.Description)
    </div>
    <div class="form-group has-feedback row">
        @Html.EditorFor(m => m.StatusId)
        @Html.EditorFor(m => m.CategoryId)
    </div>
    <div class="form-group has-feedback row">
        @Html.EditorFor(m => m.EquipModel)
        @Html.EditorFor(m => m.Manufacturer)
    </div>
    <div class="form-group has-feedback row">
        @Html.EditorFor(m => m.VINNumber)
        @Html.EditorFor(m => m.ModelYr)
    </div>
    <div class="form-group has-feedback row">
        @Html.EditorFor(m => m.TXTagId)
    </div>
</form>
<script>
    $('#@formId').ready(function () { Form().init($('#@formId')); });
</script>