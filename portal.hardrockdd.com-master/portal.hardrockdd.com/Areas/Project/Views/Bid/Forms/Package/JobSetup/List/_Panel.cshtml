@model portal.Areas.Project.Models.Bid.Package.JobListViewModel
@{
    var panelId = string.Format("panel_{0}", Guid.NewGuid());
}

<div class="panel panel-default"
     data-reloadurl="@Url.Action("PackageJobSetupListTable", "Bid", new { bdco = Model.BDCo, bidId = Model.BidId })"
     data-reloadpanels="BoreJobJobListViewModel">
    <div class="panel-heading pointer-cursor" data-toggle="collapse" data-target="#@(panelId)">
        <div class="panel-heading-btn">
            <a href="javascript:;" class="btn btn-xs btn-icon btn-circle btn-success" data-click="panel-reload"><i class="fa fa-redo"></i></a>
        </div>
        <div class="panel-title">
            <i class="fa fa-circle fa-fw text-blue mr-2 f-s-8"></i> Bid Package Job Assignment(s)
        </div>
    </div>
    <div class="panel-body show" id="@panelId">
        @Html.Partial("Forms/Package/JobSetup/List/_Table", Model)
    </div>
</div>
